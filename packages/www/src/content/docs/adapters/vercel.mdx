---
title: Vercel Adapter
shortTitle: Vercel
description: Host an httpc api with Vercel serverless functions
status: working in progress
---

import Aside from "~/components/Aside.astro";

## Quick usage
Create a `[[...slug]].ts` file inside your `pages/api` directory.

```ts
import { createHttpCVercelAdapter } from "@httpc/adapter-vercel";
import calls from "./_calls";

export default createHttpCVercelAdapter({
    calls: 
    log: true
})
```

