---
export interface Props {
  className?: string;
  prefix?: string;
  text: string;
}

const { className, prefix, text } = Astro.props;
---

<span class={`tag-badge ${className || ""}`}>
  {prefix && <span class="prefix">{prefix}</span>}
  <span class="value">{text}</span>
</span>
